// AREDN raw-script loader for the selected MeshCore serial backend.
// require() evaluates this file as a script; the returned namespace is the
// only transport brought into the module graph by the selector.
import * as backend from "meshcore_serial_api";

return backend;
