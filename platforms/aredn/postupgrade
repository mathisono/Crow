#!/bin/sh

if [ -f /tmp/crow.conf.pre-upgrade ]; then
    cp -p /tmp/crow.conf.pre-upgrade /usr/local/crow/crow.conf
    rm -f /tmp/crow.conf.pre-upgrade
fi

/etc/init.d/crow restart
/etc/init.d/crow-watchdog restart
